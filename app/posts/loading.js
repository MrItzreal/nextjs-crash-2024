const Loading = () => {
  return <LoadingSkeleton />;
};

export default Loading;

/*NOTES:
-You can add any UI inside Loading, including a skeleton or spinnner.

-The purpose of this is to place a piece of content inside a page while the "real" content loads*/
